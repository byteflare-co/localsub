# manual-testing 学習ファイル（LocalSub）

動作確認で得た再発可能な失敗の型を置く。run 開始時に読み、該当する型を実行計画へ載せる。

## 失敗の型

現在、未昇格の項目はない。

## TestID へ昇格済み

- 焼き込み出力に背景だけが表示され字幕グリフが欠落する型 → `DSK-N-02` と自動統合テスト
- 既存ファイル／symlink を暗黙上書きする型 → `DSK-E-04`、`CLI-E-03` と自動統合テスト
- 取消・動画切替後に古い翻訳結果が混入する型 → `DSK-E-03`

## archived

なし。
